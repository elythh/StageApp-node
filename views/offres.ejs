<!doctype html>
<html>
    <head>
    <!-- ... -->
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="/views/style.css" rel="stylesheet">
    <script src="https://unpkg.com/axios/dist/axios.min.js"></script>
    <head>
        <%- include('partials/header'); %>
    </head>
    </head>
    <body class="font-Poppins bg-gray-50">
        <div class="container w-full mb-20">
            <div class="container flex flex-col list">
                    <% offres.forEach(function(offre) { %>
                        <div class="bg-white text-base w-5/6 md:w-1/2 lg:w-1/3 rounded-lg shadow-lg m-auto border-b-8 border-black mt-5 card">
                            <div class="p-4 flex flex-col"> Titre de l'offre :  <%= offre.titre %></div>
                            <h3 class="mx-4 text-4xl"> Entreprise : <%= offre.entrepriseName %> </h3>
                            <div class="mx-4 text-gray-800"> Type  : <%= offre.competences %></div>
                            <p class=" mx-4 my-4">Durée : <%= offre.contenu  %></p>
                        </div>
                     <% }); %>
            </div>
        </div>
        <footer class="w-full bg-green-100 bottom-0">
            <div class="container text-center">
              2020-2021
        </footer>
        </table>
    </body>

</html>